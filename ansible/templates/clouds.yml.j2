clouds:
  undercloud:
   auth:
    username: admin
    password: "{{ undercloud_password }}"
    project_name: admin
    auth_url: "{{ auth_url }}"
    user_domain_name: Default
    project_domain_name: Default
